<!-- Table -->
<div class="table-responsive">
<table class="table">
<tr>
	<th><?php echo $this->translate('Shrine');?></th>
	<th><?php echo $this->translate('Website');?></th>
	<th><?php echo $this->translate('Photo?');?></th>
	<th><?php echo $this->translate('Opening hours?');?></th>
	<th><?php echo $this->translate('Events?');?></th>
	<th><?php echo $this->translate('Name translated?');?></th>
</tr>
<?php 
$yes = $this->translate('Yup!');
$no = $this->translate('Nope :-(');
foreach ($this->objects as $entity) :?>
<tr>
	<td><?= $this->formatEntity('association', $entity);?></td>
	<!-- <td><?= $this->formatEntity('association', $entity, ['display' => 'kind']);?></td> -->
	<td><?php foreach ($entity['urls'] as $url) {
	    if ('Map' !== $url['label']) {
	       echo $this->formatUrlObject($url);
	       break;
	    }
	}?></td>
	<td><?= !empty($entity['photos']) ? $yes : $no; ?></td>
	<td><?= isset($entity['openingHoursJson']) || isset($entity['openingHoursHuman']) ? $yes : $no; ?></td>
	<td><?= isset($entity['eventsJson']) || isset($entity['eventsHuman']) ? $yes : $no; ?></td>
	<td><?= $entity['hasTranslationCount'] . '/'.$entity['needTranslationCount']; 
	if ($this->isAllowed('route/jtranslate/phrase/edit')) :
    	foreach ($entity['translationPhrases'] as $phraseId => $phraseText) :
    	?><a href="<?= $this->url('jtranslate/phrase/edit', ['phrase_id' => $phraseId])
    	?>"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
    	<?php endforeach;
	endif;?></td>
</tr>
<?php endforeach;?>
</table>
</div>