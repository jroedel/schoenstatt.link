# Documentation

| document | what it covers |
| --- | --- |
| [DEPLOY.md](DEPLOY.md) | how a deploy actually runs: the atomic release layout and symlink swap, why the swap needs an opcode-cache reset and a revision check before any migration, the migration ledger and its phases, what happens when a remote call stops answering (timeouts, keepalives, the heartbeat), server facts, post-deploy smoke checks, rollback and when it is refused |
| [incident-2026-08-17-stale-opcache.md](incident-2026-08-17-stale-opcache.md) | the 40-minute outage a destructive migration caused by meeting a stale opcode cache: why a release symlink swap is invisible to OPcache, why three independent pools made recovery look random, why the automatic rollback made it permanent, the diagnosis that was wrong and the one field that settled it, and the four protections added afterwards |
| [exception-reporting.md](exception-reporting.md) | how production failures reach your inbox, the on-disk store, the capture/privacy profile, and the `fetch-` / `clear-exceptions.sh` scripts |
| [caching.md](caching.md) | the APCu layer: why an oversized cache item wipes the whole segment, the `max_cached_item_size` budget, `/sm/cache-status`, and what to watch when adding a cached call — plus the OPcache half, including why the **append-only interned-strings buffer** cannot be read without its segment's uptime beside it |
| [caching-performance.md](caching-performance.md) | **measured**, 2026-08-22: what the caches actually do. The persistent cache writes **no data at all** under the front controller production runs — 348 data-key reads, 0 hits — because its only write path is an `MvcEvent::FINISH` listener a Symfony route never reaches; the same pages hit 83% through the laminas kernel. Also: six full-table ACL statements on every page whatever the page is, a write budget of 1 that has skipped 24,945 writes, why a fully working cache would still only remove 14% of query time, and why nobody noticed (wall time went *down*). The evidence base for the cache refactor; harness in `tools/perf/` |
| [api-v3.md](api-v3.md) | the read/write API for automated agents: shrines and translation phrases, how a bot gets a token, why each resource has its **own** role, the field contract, and why validation is the web form's |
| [api-change-requests-response.md](api-change-requests-response.md) | the answer to the ten requests the translation agent filed on 2026-08-10: what changed, the two places its diagnosis was wrong (association public notes are translated *by design*; only one of the two corpus routes was live), and the API additions it should start using — the history endpoint, `_note`, and why "fill gaps, never overwrite" can relax |
| [strangler.md](strangler.md) | the Symfony/laminas-mvc split: which front controller is live and how to switch (**both are Symfony** since the 2026-08-11 deploy — `curl /_health` to confirm rather than trusting any document), what the bridge preserves and why, and how to add a Symfony route. **Start at "What is left, and in what order"** if the question is what to port next: 19 laminas routes remain and only two are pages, so the classification matters more than the count |
| [authorization-migration.md](authorization-migration.md) | access control across the laminas/Symfony split: how a ported route declares which ACL resource governs it, the two denial branches, the traps (getIdentity() is not the identity, isAllowed() starts a session, a null role means public), and how the bridge makes the eventual Security migration incremental |
| [sitemap.md](sitemap.md) | the static sitemap: why every file must sit at the docroot root (the directory rule that voided all 36,730 URLs), what is excluded and by which of the three access rules, why `<lastmod>` comes from `sch_changes` and not from `UpdatedOn`, the cron entry and the missing-file fallback |
| [view-scripts.md](view-scripts.md) | migrating `.phtml` view scripts to Twig: what a Symfony-served request loses, which view helpers survive without an MvcEvent, the locale trap, and a running log of what each port taught us |
| [translation.md](translation.md) | how the site gets translated: the discovery loop and why the listener is the only door into `trans_phrases`, text domains across the two front controllers, the checklist for writing a Twig template (never translate record data, user input or a concatenated message), retirement vs retraction, and a log of every way this has broken so far |
| [translation-migration.md](translation-migration.md) | superseded by translation.md for how translation works; kept for the still-live argument about JTranslate's *future scope* — what 3.0 removes, what `symfony/translation` should take over, and why the precompiled-PHP-array format stays |
| [database-charset.md](database-charset.md) | the InnoDB + `utf8mb4_unicode_520_ci` standard: why that collation and not MySQL's or MariaDB 11's default, why the connection charset is half the job, which 16 tables are server maintenance rather than migrations, and the traps (error 1833 vs `FOREIGN_KEY_CHECKS`, silent `TEXT` promotion, how to actually test for double-encoded latin1) |
| [php-85.md](php-85.md) | PHP 8.5 and the dependency ceiling behind it: why no laminas-mvc version admits it, why the platform pin stays at production's version, why the same obstacle blocks FrameworkBundle — and the **per-account `php.ini` baseline**: every tuned value with its current and wanted setting, since a konsoleH version switch silently reverts them all |
| [libraries.md](libraries.md) | the lending feature: how one system serves an internal study house, a university library and an open one; why a "Public" library is open to every signed-in account **on purpose** (unrecorded loans are the worse problem); why borrowers are not accounts and cannot be; the Patres person import and what it can and cannot overwrite; **how deleting a library works and why it could not simply reuse SionModel's generic delete** (only one of four child tables has a foreign key, so the generic one orphans rather than deletes); and what was never built |
| [library-imports.md](library-imports.md) | cataloguing in a spreadsheet: the template a librarian downloads, how columns are matched from their own headings, the three rules that decide what an import does (the barcode identifies the copy, a blank cell erases, a complete import retires what is missing), why a linked literature record overrides the spreadsheet, the four defects the old engine had silently, and when to run it from the console |
| [timeline-and-corpus.md](timeline-and-corpus.md) | the Fr. Kentenich timeline and the Schmiedl text corpus: what the 527 events and 2,753 texts actually contain, why the link between them was never made, the design decisions already settled (one table with a `kind` discriminator, a single FK, an importer rather than a form), and the two-part plan — **Part 1 is done, and its last item was closed on 2026-09-08 by deleting the four write routes rather than guarding them**; Part 2 waits for laminas to be gone and for two open product decisions |
| [shrine-data.md](shrine-data.md) | the plan for a shrine/association dataset good enough to distribute to schoenstatt.com and schoenstatt-fathers.org: why the constraint is verification throughput and not field capacity (216 of 250 shrines last written in 2019), and three workstreams — **the column set** (which of 78 columns a shrine earns; `Latitude`/`Longitude` duplicate `Location` on 247 rows), **correctness and completeness** (provenance, built and deployed; suggestions, not yet; and the authority chain that is modelled but empty — 1 of 250 shrines has a role assignment), and **autonomous agents** (blocked on reach: 64 of 250 shrines have any email address). Plus the four decisions taken and why the contributor UI is gated on Bootstrap 5 |
| [reviews.md](reviews.md) | the review/comment feature — started, unfinished, **tabled**: what it is (a visitor's impression of a record) and emphatically is not (an edit proposal — the two were conflated once, and the codebase carries two separate status vocabularies), the rating half that was never built, why shrines cannot be reviewed for want of a predicate, and the live exposure — any address that can receive mail can publish text with no review, because both controllers hardcode `published` and the `in-review` status has never been used |
| [BACKLOG.md](BACKLOG.md) | the forward-only modernization roadmap — strategic direction (Symfony via strangler), queued work, open bugs and decisions |
| [acl-rules.md](acl-rules.md) | generated: every role, route guard and rule, with inheritance expanded and Symfony-served routes flagged. Regenerate with `tools/acl-table.php` and diff after any authorization change |
| [acl-baseline.json](acl-baseline.json) | generated: the same facts sorted for `diff`. The parity oracle an authorization change is checked against |
| [history.md](history.md) | the modernization journal — closed work, corrections, and reusable techniques (golden-master diffing, cycle detection, deploy-day lessons) |

Operational instructions for working *on* this codebase — the local Docker
capsule, how to run the test suites, coding standard, architecture — live in
[`../CLAUDE.md`](../CLAUDE.md), which is the single source of truth for those and
is kept current deliberately.
